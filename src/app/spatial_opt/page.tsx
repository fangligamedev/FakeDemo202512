"use client";
import InfiniteCanvasOpt from '@/components/demo/InfiniteCanvasOpt';

export default function SpatialPageOpt() {
    return (
        <main className="min-h-screen bg-black">
            <InfiniteCanvasOpt />
        </main>
    );
}
